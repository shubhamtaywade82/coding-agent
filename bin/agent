#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/coding_agent"

query = ARGV.join(" ")
abort("Usage: agent <task>") if query.empty?

# Run the agent
# This wires ollama-client + agent_runtime automatically
CodingAgent.run(query)
